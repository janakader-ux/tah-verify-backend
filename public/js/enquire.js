(()=>{'use strict';const form=document.getElementById('enquiryForm'),status=document.getElementById('enquiryStatus'),estimate=document.getElementById('enquiryEstimate');const API='https://tah-verify-backend.onrender.com';function totals(){const u=Number(form.elements.uk_count.value),o=Number(form.elements.overseas_count.value);estimate.textContent=form.elements.service.value==='bulk'?(u+o<10?'Minimum 10 applicants in total.':'Indicative standard remote total: £'+(u*39+o*85).toLocaleString('en-GB')+'. Scope and any larger-volume pricing will be confirmed by email.'):'We will agree a quote before booking.';}form.addEventListener('input',totals);totals();form.addEventListener('submit',async e=>{e.preventDefault();const b=form.querySelector('button');b.disabled=true;status.textContent='Saving your enquiry…';try{const data=Object.fromEntries(new FormData(form));data.uk_count=Number(data.uk_count);data.overseas_count=Number(data.overseas_count);const r=await fetch(API+'/api/enquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await r.json();if(!r.ok)throw new Error(typeof result.detail==='string'?result.detail:'Please check your entries.');status.textContent=result.message+' Reference: '+result.reference+(result.notification_sent?'':' Please also email us quoting this reference so we can follow up promptly.');}catch(e){status.textContent=e.message;}finally{b.disabled=false;}});})();